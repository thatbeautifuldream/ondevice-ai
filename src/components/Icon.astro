---
import { ICONS } from "../lib/icons";

interface Props {
	name: string;
	class?: string;
}

const { name, class: cls = "size-4" } = Astro.props;
const inner = ICONS[name] ?? "";
---

<svg viewBox="0 0 16 16" fill="none" class={cls} aria-hidden="true" set:html={inner} />
